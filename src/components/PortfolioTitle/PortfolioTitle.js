import React from 'react';
import './PortfolioTitle.css';

const PortfolioTitle = () => (
  <div className="title" data-testid="PortfolioTitle">
    <h1>Portfolio App</h1>
  </div>
);

PortfolioTitle.propTypes = {};

PortfolioTitle.defaultProps = {};

export default PortfolioTitle;
